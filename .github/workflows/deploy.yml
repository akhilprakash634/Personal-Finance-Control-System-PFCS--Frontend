name: Deploy Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Copy files via SCP
        uses: appleboy/scp-action@master
        with:
          host: 43.205.229.25
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          source: "."
          target: "~/Personal-Finance-Control-System-PFCS/frontend"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 43.205.229.25
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          script: |
            cd ~/Personal-Finance-Control-System-PFCS/frontend
            # Copy secrets from the server's secret folder
            cp ~/secret/frontend/.env .env
            # Try docker compose (v2) first, then fallback to docker-compose
            (sudo docker compose up --build -d frontend) || (sudo docker-compose up --build -d frontend)
